function intervals = interval_of_roots(func, a, b )
%finds subintervals of [a b] that bolzano condition aplly in them, means
%that finds subintervals with one root in them.
%   Detailed explanation goes here


end

